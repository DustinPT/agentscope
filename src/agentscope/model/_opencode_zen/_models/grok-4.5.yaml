name: grok-4.5
label: Grok 4.5
status: active

input_types:
  - text/plain
  - image/jpeg
  - image/png

output_types:
  - text/plain
  - application/x-thinking

context_size: 1000000
output_size: 30000

runtime_init_kwargs:
  api_style: chat_completions

parameter_overrides:
  max_tokens:
    maximum: 30000
