name: glm-5.1
label: GLM 5.1
status: active

input_types:
  - text/plain
  - application/x-thinking

output_types:
  - text/plain
  - application/x-thinking

context_size: 200000
output_size: 128000

runtime_init_kwargs:
  api_style: chat_completions

parameter_overrides:
  max_tokens:
    maximum: 128000
